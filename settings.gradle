pluginManagement {
    repositories {
        maven { url "https://plugins.gradle.org/m2/" }
        google()
        jcenter()
        mavenCentral()
        gradlePluginPortal()
    }
}

rootProject.name = 'kafka-connect-hdfs-bundle'
